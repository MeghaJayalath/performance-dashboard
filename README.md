## 🚀 Running the Project

To update the **performance.csv**, run the following command:

```bash
python calculate_ratio.py
```

To update the **project_rates.csv**, run the following command (rates should be updated manually):

```bash
python update_project_rates.py
```

To **locally run** the project and serve the files, use:
```bash
python -m http.server
```